# ---- Trend Prediction Prompts ----

TREND_SYSTEM_PROMPT = """你是一个行业趋势分析师。请基于当前市场数据预测岗位未来的发展趋势。
输出JSON格式，包含：薪资预测（未来3年）、需求量预测、热门技能、行业前景评分。"""

TREND_USER_PROMPT = """岗位名称：{job_name}
用户当前技能：{skills}

请预测该岗位未来3年的发展趋势：
- 薪资趋势（年份: 2026, 2027, 2028，对应年薪范围）
- 需求量趋势（增长/持平/下降）
- 未来热门技能TOP5
- 行业前景评分（0-100）"""

# ---- Career Path Prompts ----

PATH_SYSTEM_PROMPT = """你是一个职业规划师。请为用户规划从当前岗位到目标岗位的职业发展路径。
输出JSON格式，包含分阶段的职业规划。"""

PATH_USER_PROMPT = """用户当前状态：
{user_profile}

目标岗位：{job_name}

可参考的晋升路径：{promotion_data}

请规划一个包含3-5个阶段的职业发展路径。每个阶段包含：
- 阶段名称
- 时间节点
- 核心目标
- 需要掌握的能力
- 预期薪资范围

输出JSON格式：
{{
  "current": "当前定位",
  "target": "目标岗位",
  "phases": [
    {{"阶段": "阶段名称", "时间": "预计时间", "目标": "...", "能力要求": "...", "薪资预期": "..."}}
  ]
}}"""

SELF_REFLECT_PROMPT = """你刚生成了以下职业规划路径：
{output}

请自我检查：
1. 各阶段时间节点是否合理？跨度不要过大或过小。
2. 薪资预期是否与当前市场水平一致？
3. 能力要求是否具体、可衡量？
4. 各阶段之间的递进是否逻辑清楚？

如有问题，直接输出修正后的完整职业规划JSON。如无问题，在target字段末尾加"(已自检)"。
"""
